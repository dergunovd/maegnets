import styled from "@emotion/styled";

import logo from '../assets/logo.svg';

export const Logo = styled.div`
    width: 232px;
    height: 67px;
    background: url('${logo}') center center / contain;
`;
